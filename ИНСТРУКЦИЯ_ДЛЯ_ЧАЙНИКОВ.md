# 📚 ПОЛНАЯ ИНСТРУКЦИЯ: Как залить сайт на Spaceweb (для начинающих)

## 🎯 Что мы будем делать:
1. Подготовим файлы на компьютере
2. Создадим базу данных на Spaceweb
3. Загрузим файлы на сервер
4. Настроим сайт

---

## ШАГ 1: Подготовка файлов на компьютере

### 1.1. Откройте папку с проектом
Найдите папку `curs` на рабочем столе и откройте её.

### 1.2. Проверьте, что есть все папки:
- `app`
- `public`
- `config`
- `database`
- `resources`
- `routes`

✅ Если всё есть - продолжаем!

---

## ШАГ 2: Получение данных от Spaceweb

### 2.1. Войдите в панель управления Spaceweb
- Откройте сайт spaceweb.ru
- Войдите в личный кабинет
- Найдите раздел "Панель управления" или "ISPmanager"

### 2.2. Найдите данные FTP
Ищите раздел с названием:
- "FTP" 
- "Файлы"
- "Файловый менеджер"

**Что нужно записать:**
- FTP хост (например: `ftp.yourdomain.com` или IP адрес)
- FTP логин (ваш логин)
- FTP пароль (ваш пароль)
- Порт (обычно 21)

📝 **Запишите эти данные в блокнот!**

---

## ШАГ 3: Установка FileZilla (если нет)

### 3.1. Скачайте FileZilla
- Откройте сайт: https://filezilla-project.org/
- Скачайте FileZilla Client (бесплатная версия)
- Установите программу

### 3.2. Откройте FileZilla

---

## ШАГ 4: Подключение к серверу через FileZilla

### 4.1. Введите данные в FileZilla
В верхней части окна FileZilla найдите поля:
- **Хост:** вставьте FTP хост (из шага 2.2)
- **Имя пользователя:** вставьте FTP логин
- **Пароль:** вставьте FTP пароль
- **Порт:** оставьте 21

### 4.2. Нажмите "Быстрое соединение"
- Должно появиться сообщение "Список каталогов успешно получен"
- ✅ Если видите папки на сервере - всё хорошо!

---

## ШАГ 5: Поиск папки для загрузки файлов

### 5.1. Найдите папку вашего сайта
В правой части FileZilla (это сервер) найдите одну из папок:
- `public_html`
- `www`
- `htdocs`
- Или папку с названием вашего домена

### 5.2. Откройте эту папку
Дважды кликните на папку, чтобы открыть её.

**ВАЖНО:** В этой папке должны быть файлы вашего сайта (если сайт уже был).

---

## ШАГ 6: Загрузка файлов проекта

### 6.1. Откройте папку проекта на компьютере
В левой части FileZilla (это ваш компьютер):
- Найдите папку `curs` на рабочем столе
- Откройте её

### 6.2. Выберите все файлы и папки
- Нажмите `Ctrl+A` (выбрать всё)
- Или выделите мышкой все файлы

### 6.3. Загрузите файлы
- Правой кнопкой мыши → "Загрузить на сервер"
- Или перетащите файлы из левой части в правую
- ⏳ Подождите, пока загрузятся все файлы (может занять несколько минут)

✅ **Готово!** Файлы загружены.

---

## ШАГ 7: Создание базы данных

### 7.1. Откройте раздел "Базы данных"
В панели Spaceweb найдите:
- "Базы данных MySQL"
- "MySQL"
- "Базы данных"

### 7.2. Создайте новую базу данных
Нажмите кнопку "Создать" или "Добавить"

**Заполните:**
- **Имя базы данных:** `commercial_proposals` (или любое другое)
- **Имя пользователя:** создайте нового (например: `db_user`)
- **Пароль:** придумайте надёжный пароль (запишите!)

### 7.3. Сохраните данные
📝 **Запишите в блокнот:**
- Имя базы данных: ________________
- Имя пользователя БД: ________________
- Пароль БД: ________________
- Хост БД: обычно `localhost` (запишите, что написано)

---

## ШАГ 8: Выполнение миграций (создание таблиц)

### 8.1. Откройте phpMyAdmin
В панели Spaceweb найдите:
- "phpMyAdmin"
- Или ссылку на управление БД

### 8.2. Войдите в phpMyAdmin
- Используйте те же данные, что и для БД (из шага 7)

### 8.3. Выберите вашу базу данных
В левом меню нажмите на название вашей БД (из шага 7.2)

### 8.4. Откройте вкладку "SQL"
Вверху страницы найдите вкладку "SQL" и нажмите на неё.

### 8.5. Выполните SQL запросы
Скопируйте и выполните по очереди каждый SQL запрос:

**Запрос 1:**
```sql
CREATE TABLE `users` (
  `id` bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `email` varchar(255) NOT NULL,
  `email_verified_at` timestamp NULL DEFAULT NULL,
  `password` varchar(255) NOT NULL,
  `role` enum('guest','user','admin') NOT NULL DEFAULT 'user',
  `remember_token` varchar(100) DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `users_email_unique` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Запрос 2:**
```sql
CREATE TABLE `password_reset_tokens` (
  `email` varchar(255) NOT NULL,
  `token` varchar(255) NOT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Запрос 3:**
```sql
CREATE TABLE `templates` (
  `id` bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) UNSIGNED DEFAULT NULL,
  `title` varchar(255) NOT NULL,
  `description` text DEFAULT NULL,
  `body_html` longtext NOT NULL,
  `variables` json DEFAULT NULL,
  `is_system` tinyint(1) NOT NULL DEFAULT 0,
  `is_published` tinyint(1) NOT NULL DEFAULT 0,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  `deleted_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `templates_user_id_foreign` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Запрос 4:**
```sql
CREATE TABLE `proposals` (
  `id` bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) UNSIGNED NOT NULL,
  `template_id` bigint(20) UNSIGNED DEFAULT NULL,
  `title` varchar(255) NOT NULL,
  `offer_number` varchar(255) DEFAULT NULL,
  `offer_date` date NOT NULL,
  `seller_info` text DEFAULT NULL,
  `buyer_info` text DEFAULT NULL,
  `body_html` longtext NOT NULL,
  `currency` varchar(10) NOT NULL DEFAULT '₽',
  `vat_rate` decimal(5,2) NOT NULL DEFAULT 0.00,
  `terms` text DEFAULT NULL,
  `status` enum('draft','published') NOT NULL DEFAULT 'draft',
  `published_at` timestamp NULL DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  `deleted_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `proposals_user_id_foreign` (`user_id`),
  KEY `proposals_template_id_foreign` (`template_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Запрос 5:**
```sql
CREATE TABLE `proposal_items` (
  `id` bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
  `proposal_id` bigint(20) UNSIGNED NOT NULL,
  `name` varchar(255) NOT NULL,
  `quantity` decimal(10,2) NOT NULL DEFAULT 1.00,
  `unit` varchar(20) NOT NULL DEFAULT 'шт.',
  `price` decimal(10,2) NOT NULL,
  `discount` decimal(5,2) NOT NULL DEFAULT 0.00,
  `sort_order` int(11) NOT NULL DEFAULT 0,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `proposal_items_proposal_id_foreign` (`proposal_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Запрос 6:**
```sql
CREATE TABLE `proposal_files` (
  `id` bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
  `proposal_id` bigint(20) UNSIGNED DEFAULT NULL,
  `user_id` bigint(20) UNSIGNED NOT NULL,
  `type` enum('logo','image') NOT NULL DEFAULT 'image',
  `original_name` varchar(255) NOT NULL,
  `path` varchar(255) NOT NULL,
  `mime_type` varchar(255) NOT NULL,
  `size` int(11) NOT NULL,
  `meta` json DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `proposal_files_proposal_id_foreign` (`proposal_id`),
  KEY `proposal_files_user_id_foreign` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Запрос 7:**
```sql
CREATE TABLE `personal_access_tokens` (
  `id` bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
  `tokenable_type` varchar(255) NOT NULL,
  `tokenable_id` bigint(20) UNSIGNED NOT NULL,
  `name` varchar(255) NOT NULL,
  `token` varchar(64) NOT NULL,
  `abilities` text DEFAULT NULL,
  `last_used_at` timestamp NULL DEFAULT NULL,
  `expires_at` timestamp NULL DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `personal_access_tokens_token_unique` (`token`),
  KEY `personal_access_tokens_tokenable_type_tokenable_id_index` (`tokenable_type`,`tokenable_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

✅ **После каждого запроса должно появиться сообщение "Запрос успешно выполнен"**

---

## ШАГ 9: Создание файла .env на сервере

### 9.1. Откройте файловый менеджер Spaceweb
В панели Spaceweb найдите "Файловый менеджер" или "Файлы"

### 9.2. Найдите папку с вашим сайтом
Откройте папку `public_html` (или ту, куда загружали файлы)

### 9.3. Создайте новый файл
- Нажмите "Создать" → "Файл"
- Имя файла: `.env`

### 9.4. Откройте файл для редактирования
Дважды кликните на файл `.env`

### 9.5. Вставьте следующий текст:
```env
APP_NAME="Commercial Proposals"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://ваш-домен.com

DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=commercial_proposals
DB_USERNAME=ваш_пользователь_бд
DB_PASSWORD=ваш_пароль_бд

FILESYSTEM_DRIVER=local
SESSION_DRIVER=file
```

**ВАЖНО:** Замените:
- `ваш-домен.com` → ваш реальный домен (например: `mysite.ru`)
- `commercial_proposals` → имя вашей БД (из шага 7.2)
- `ваш_пользователь_бд` → имя пользователя БД (из шага 7.2)
- `ваш_пароль_бд` → пароль БД (из шага 7.2)

### 9.6. Сохраните файл
Нажмите "Сохранить"

---

## ШАГ 10: Установка зависимостей (Composer)

### Вариант А: Если есть SSH доступ

### 10.1. Откройте SSH терминал
В панели Spaceweb найдите "SSH" или "Терминал"

### 10.2. Перейдите в папку сайта
Введите команду:
```bash
cd public_html
```

### 10.3. Установите зависимости
Введите команду:
```bash
composer install --no-dev
```

⏳ Подождите завершения (может занять несколько минут)

---

### Вариант Б: Если нет SSH (установка вручную)

### 10.1. Установите Composer на свой компьютер
- Скачайте: https://getcomposer.org/download/
- Установите Composer

### 10.2. Откройте терминал/командную строку
На Windows: нажмите Win+R, введите `cmd`
На Mac: откройте "Терминал"

### 10.3. Перейдите в папку проекта
```bash
cd Desktop/curs
```

### 10.4. Установите зависимости
```bash
composer install --no-dev
```

### 10.5. Загрузите папку vendor на сервер
Через FileZilla загрузите папку `vendor` на сервер в ту же папку, где остальные файлы.

---

## ШАГ 11: Настройка прав доступа

### 11.1. Откройте файловый менеджер Spaceweb
Найдите "Файловый менеджер"

### 11.2. Найдите папку `storage`
В папке с вашим сайтом найдите папку `storage`

### 11.3. Установите права
- Правой кнопкой на папку `storage` → "Права доступа" или "Chmod"
- Установите: `755`
- Нажмите "Применить"

### 11.4. То же самое для `bootstrap/cache`
- Найдите папку `bootstrap`
- Откройте её
- Найдите папку `cache`
- Установите права: `755`

---

## ШАГ 12: Создание первого администратора

### 12.1. Откройте phpMyAdmin
(Как в шаге 8.1)

### 12.2. Выберите вашу базу данных
(Как в шаге 8.3)

### 12.3. Откройте вкладку "SQL"

### 12.4. Выполните этот запрос:
```sql
INSERT INTO users (name, email, password, role, created_at, updated_at) 
VALUES (
    'Администратор', 
    'admin@example.com', 
    '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', 
    'admin', 
    NOW(), 
    NOW()
);
```

**ВАЖНО:** 
- Замените `admin@example.com` на ваш реальный email
- Пароль по умолчанию: `password` (смените после первого входа!)

### 12.5. Нажмите "Вперёд"
✅ Должно появиться "1 строка добавлена"

---

## ШАГ 13: Настройка PHP версии

### 13.1. Откройте настройки PHP
В панели Spaceweb найдите:
- "Настройки PHP"
- "Версии PHP"
- "PHP"

### 13.2. Выберите PHP 8.1 или выше
- Найдите ваш домен
- Выберите PHP версию: 8.1, 8.2 или 8.3
- Сохраните

---

## ШАГ 14: Проверка работы сайта

### 14.1. Откройте ваш сайт в браузере
Введите в адресной строке: `https://ваш-домен.com`

### 14.2. Что должно произойти:
- Откроется страница входа
- Или редирект на `/login`

### 14.3. Войдите
- Email: тот, что указали в шаге 12.4
- Пароль: `password`

✅ **Если вошли - всё работает!**

---

## 🆘 Если что-то не работает:

### Проблема: "500 Internal Server Error"
**Решение:**
1. Проверьте файл `.env` - все ли данные правильные
2. Проверьте права на папки `storage` и `bootstrap/cache` (должны быть 755)
3. Проверьте логи ошибок в панели Spaceweb

### Проблема: "Database connection failed"
**Решение:**
1. Проверьте данные в `.env` (имя БД, пользователь, пароль)
2. Убедитесь, что БД создана
3. Проверьте хост БД (может быть не `localhost`, а IP адрес)

### Проблема: Страница белая или ошибка
**Решение:**
1. Включите отладку временно: в `.env` измените `APP_DEBUG=true`
2. Обновите страницу - увидите ошибку
3. Исправьте ошибку
4. Верните `APP_DEBUG=false`

### Проблема: CSS/JS не загружаются
**Решение:**
1. Проверьте, что файлы загружены в `public/css/` и `public/js/`
2. Проверьте права на файлы (должны быть 644)

---

## ✅ Чеклист готовности:

- [ ] Файлы загружены на сервер
- [ ] База данных создана
- [ ] Таблицы созданы (миграции выполнены)
- [ ] Файл `.env` создан и заполнен
- [ ] Зависимости установлены (папка `vendor`)
- [ ] Права на папки установлены (755)
- [ ] Администратор создан
- [ ] PHP версия 8.1+
- [ ] Сайт открывается в браузере
- [ ] Можно войти

---

## 📞 Нужна помощь?

Если застряли на каком-то шаге:
1. Перечитайте инструкцию для этого шага
2. Проверьте логи ошибок в панели Spaceweb
3. Обратитесь в техподдержку Spaceweb

**Удачи! 🚀**



