# Быстрая настройка автоматического деплоя

## Что уже сделано ✅

1. ✅ Проект загружен на GitHub: https://github.com/yarrobong/kp
2. ✅ Созданы скрипты деплоя
3. ✅ Создана документация

## Что нужно сделать сейчас

### Шаг 1: Добавить workflow файл в GitHub

Токен не имеет прав для автоматического создания workflow. Добавьте файл вручную:

1. Откройте https://github.com/yarrobong/kp
2. Нажмите **Add file** → **Create new file**
3. Введите путь: `.github/workflows/deploy.yml`
4. Откройте файл `ADD_WORKFLOW_MANUAL.md` в проекте и скопируйте содержимое YAML
5. Вставьте в файл на GitHub и нажмите **Commit new file**

### Шаг 2: Настроить секреты в GitHub

1. Откройте https://github.com/yarrobong/kp/settings/secrets/actions
2. Нажмите **New repository secret** и добавьте:

   **SERVER_HOST:**
   ```
   178.209.127.17
   ```

   **SERVER_PASSWORD:**
   ```
   vJq*s-YNG5XQt7
   ```

   **DEPLOY_PATH:**
   ```
   /var/www/html
   ```
   *(Или путь, куда вы установите проект на сервере)*

### Шаг 3: Настроить проект на сервере

Подключитесь к серверу:

```bash
ssh root@178.209.127.17
```

Выполните:

```bash
# Перейдите в директорию сайта (или создайте)
cd /var/www/html

# Клонируйте репозиторий
git clone https://github.com/yarrobong/kp.git .

# Установите зависимости
composer install --no-dev --optimize-autoloader

# Настройте права
chmod -R 755 storage bootstrap/cache

# Создайте .env файл с настройками БД
nano .env
```

### Шаг 4: Проверить работу

1. Сделайте любой коммит и push:
```bash
git add .
git commit -m "Test deployment"
git push origin main
```

2. Откройте https://github.com/yarrobong/kp/actions
3. Вы увидите запущенный workflow "Deploy to Server"
4. После успешного выполнения код автоматически обновится на сервере

## Готово! 🎉

Теперь при каждом push в ветку `main` код будет автоматически деплоиться на сервер.

## Дополнительная информация

- Подробная инструкция: `GITHUB_DEPLOY_SETUP.md`
- Инструкция по добавлению workflow: `ADD_WORKFLOW_MANUAL.md`

