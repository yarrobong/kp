# КП Генератор - Система автоматизации коммерческих предложений

## 🚀 Описание проекта

**КП Генератор** - это полнофункциональная веб-система для автоматизации процесса создания и управления коммерческими предложениями. Система позволяет управлять каталогом товаров, формировать профессиональные КП и экспортировать их в PDF формат.

## ✨ Возможности

### 📦 Управление товарами
- Создание, редактирование и удаление товаров
- Поддержка категорий и описаний
- Загрузка изображений товаров
- Поиск и фильтрация товаров

### 📄 Генерация коммерческих предложений
- Интуитивный интерфейс создания КП
- Автоматический расчет сумм
- Выбор товаров из каталога
- Валидация данных

### 📊 Отслеживание статусов
- Статусы предложений: Черновик, Отправлено, Принято, Отклонено
- Цветовая индикация статусов
- История изменений

### 📋 Экспорт в PDF
- Профессионально оформленные документы
- Автоматическая генерация с TCPDF
- Кастомизация внешнего вида
- Скачивание с правильными заголовками

### 🔍 Поиск и фильтры
- Умный поиск по товарам
- Фильтрация по категориям и ценам
- Сортировка результатов

## 🛠 Технический стек

- **Backend:** PHP 8.1+
- **Frontend:** HTML5, CSS3, JavaScript (ES6+)
- **База данных:** MySQL (с JSON fallback)
- **PDF генерация:** TCPDF
- **Стилизация:** Кастомный CSS с современным дизайном

## 🏗 Архитектура (MVC)

Проект построен по принципу **Model-View-Controller (MVC)**:

### 📊 Models (Модели)
Отвечают за данные и бизнес-логику:
- `Product.php` - работа с товарами
- `Proposal.php` - работа с предложениями
- `Model.php` - базовый класс модели

### 🎨 Views (Представления)
Отвечают за отображение интерфейса:
- `layouts/main.php` - основной макет
- `home/index.php` - главная страница
- `products/` - представления товаров
- `proposals/` - представления предложений

### 🎮 Controllers (Контроллеры)
Обрабатывают запросы и координируют модели и представления:
- `HomeController.php` - главная страница
- `ProductController.php` - управление товарами
- `ProposalController.php` - управление предложениями
- `Controller.php` - базовый класс контроллера

### 🛠 Core (Ядро)
- `Router.php` - маршрутизация
- `Model.php` - базовая модель
- `Controller.php` - базовый контроллер

## 📁 Структура проекта

```
├── public/                # Публичная директория
│   ├── index.php          # Точка входа приложения
│   ├── .htaccess          # Правила Apache
│   ├── css/
│   │   └── app.css        # Стили приложения
│   └── js/
│       └── app.js         # JavaScript
├── core/                  # Ядро приложения
│   ├── Router.php         # Маршрутизатор
│   ├── Model.php          # Базовая модель
│   └── Controller.php     # Базовый контроллер
├── models/                # Модели данных
│   ├── Product.php        # Модель товара
│   └── Proposal.php       # Модель предложения
├── controllers/           # Контроллеры
│   ├── HomeController.php    # Главная страница
│   ├── ProductController.php # Товары
│   └── ProposalController.php # Предложения
├── views/                 # Представления
│   ├── layouts/
│   │   └── main.php       # Основной макет
│   ├── home/
│   │   └── index.php      # Главная страница
│   ├── products/          # Представления товаров
│   └── proposals/         # Представления предложений
├── config/                # Конфигурация
│   └── database.php       # Настройки БД
├── index.php              # Перенаправление на public/
├── .htaccess              # Корневые правила Apache
├── products.json          # JSON товары (fallback)
├── proposals.json         # JSON предложения (fallback)
├── create_tables.php      # Скрипт создания БД
├── composer.json          # Зависимости
└── README.md              # Документация
```

## 🔒 Безопасность

### ⚠️ Важно: HTTP vs HTTPS
Приложение настроено для работы по HTTP с предупреждениями о безопасности для **разработки**. Для production использования **настоятельно рекомендуется** настроить HTTPS!

#### Почему это важно?
- HTTP передает данные в открытом виде
- Пароли и личные данные могут быть перехвачены
- Современные браузеры помечают HTTP сайты как "небезопасные"

#### Настройка HTTPS с Let's Encrypt (рекомендуется)
```bash
# Установите certbot
sudo apt install certbot python3-certbot-nginx

# Получите бесплатный сертификат (замените yourdomain.com)
sudo certbot --nginx -d yourdomain.com

# Certbot автоматически настроит nginx и добавит редирект HTTP → HTTPS
```

#### Ручная настройка HTTPS
```bash
# Создайте самоподписанный сертификат (для тестирования)
sudo openssl req -x509 -newkey rsa:4096 -keyout /etc/ssl/private/selfsigned.key \
  -out /etc/ssl/certs/selfsigned.crt -days 365 -nodes \
  -subj '/C=RU/ST=Moscow/L=Moscow/O=Dev/CN=yourdomain.com'

# Добавьте в nginx конфигурацию:
server {
    listen 443 ssl http2;
    server_name yourdomain.com;

    ssl_certificate /etc/ssl/certs/selfsigned.crt;
    ssl_certificate_key /etc/ssl/private/selfsigned.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    # ... остальная конфигурация
}
```

### 🛡️ Меры безопасности в приложении
- ✅ Защита от XSS атак
- ✅ Защита от CSRF атак
- ✅ Безопасные HTTP заголовки
- ✅ Валидация вводимых данных
- ✅ Хеширование паролей (bcrypt)
- ✅ Защита от SQL инъекций (PDO)

## 🚀 Установка и запуск

### Требования
- PHP 8.1 или выше
- MySQL (опционально, работает с JSON fallback)
- Composer
- Git
- Веб-сервер (Nginx/Apache)

### Быстрая установка (автоматическая)

```bash
git clone https://github.com/yarrobong/kp.git
cd kp
./setup_database.php  # Настройка БД
./deploy.sh development  # Локальный деплой
cd public && php -S localhost:8000
```

### Ручная установка

1. **Клонировать репозиторий:**
```bash
git clone https://github.com/yarrobong/kp.git
cd kp
```

2. **Установить зависимости:**
```bash
composer install
```

3. **Настроить базу данных:**
```bash
php setup_database.php
```

4. **Выполнить локальный деплой:**
```bash
./deploy.sh development
```

5. **Настроить веб-сервер:**
   - **Apache:** Указать корневую директорию на `/путь/к/проекту/`
   - **Nginx:** Настроить root на `/путь/к/проекту/public/`
   - Включить URL rewriting

### Запуск

**Через PHP built-in server:**
```bash
cd public
php -S localhost:8000
```
Затем открыть: `http://localhost:8000`

**Через Apache/Nginx:**
Указать document root на папку проекта и открыть в браузере.

## 🔄 Автоматическая синхронизация

Проект поддерживает автоматическую синхронизацию изменений с Git и деплоем на сервер.

### Режимы работы

#### 1. Ручная синхронизация
```bash
./sync_trigger.sh "Ваше сообщение коммита"
```

#### 2. Полная синхронизация с деплом
```bash
./auto_sync.sh "Сообщение коммита"
```

#### 3. Слежение за изменениями (автоматическая)
```bash
./watch_and_sync.sh
```
Останавливается по Ctrl+C

#### 4. Синхронизация с Git только
```bash
./git_sync.sh "Сообщение коммита"
```

### Настройка Production деплоя

1. **Отредактируйте `.deploy_config`:**
```bash
DEPLOY_TO_PRODUCTION=true
PRODUCTION_SERVER="your-server-ip"
PRODUCTION_USER="deploy"
PRODUCTION_PATH="/var/www/kp-generator"
```

2. **Настройте SSH доступ:**
```bash
ssh-keygen -t rsa -b 4096
ssh-copy-id deploy@your-server-ip
```

3. **Запустите деплой:**
```bash
./deploy_production.sh
```

### Структура скриптов

- `sync_trigger.sh` - быстрый триггер для мелких изменений
- `auto_sync.sh` - полная синхронизация (Git + локальный деплой + production)
- `git_sync.sh` - только Git синхронизация
- `watch_and_sync.sh` - демон слежения за файлами
- `deploy.sh` - локальный деплой
- `deploy_production.sh` - production деплой
- `.deploy_config` - конфигурация деплоя

## 🎯 Использование

### Основной рабочий процесс:

1. **Главная страница** (`/`) - ознакомление с возможностями
2. **Управление товарами** (`/products`) - создание каталога
3. **Создание КП** (`/proposals/create`) - формирование предложений
4. **Просмотр списка** (`/proposals`) - управление предложениями
5. **Экспорт в PDF** - скачивание готовых документов

### Навигация:
- **Главная** - презентация системы
- **Товары** - управление каталогом
- **КП** - коммерческие предложения
- **Выход** - возвращение на главную

## 📚 Документация

### Дополнительные руководства:
- **[WEBMASTER_SETUP.md](WEBMASTER_SETUP.md)** - Подключение и настройка инструментов вебмастеров (Яндекс.Вебмастер, Google Search Console, Bing Webmaster Tools)
  - Обоснование необходимости вебмастеров
  - Пошаговые инструкции по настройке
  - Рекомендации по использованию

### SEO оптимизация:
- ✅ Мета-теги (Title, Description, Keywords, Robots)
- ✅ Анкорные ссылки и внутренние якоря
- ✅ Страница 404 с правильной настройкой
- ✅ Файл robots.txt
- ✅ Динамический sitemap.xml
- ✅ OpenGraph разметка для социальных сетей
- ✅ Подключение инструментов вебмастеров

### Аналитика:
- ✅ **Яндекс.Метрика** подключена и работает на всех страницах
  - ID счетчика: 106018547
  - Вебвизор (запись сессий пользователей)
  - Карта кликов
  - Ecommerce отслеживание через dataLayer
  - Точный отскок и отслеживание ссылок
  - Поддержка SSR (Server-Side Rendering)

## 🔒 Безопасность

- Валидация всех входных данных
- Защита от XSS через `htmlspecialchars()`
- Проверка прав доступа к товарам
- Обработка ошибок с пользовательскими сообщениями

## 📊 API Endpoints

### Товары
- `GET /products` - список товаров
- `GET /products/create` - форма создания товара
- `POST /products/create` - создание товара
- `GET /products/{id}/edit` - форма редактирования
- `POST /products/{id}/edit` - обновление товара
- `POST /products/{id}/delete` - удаление товара

### Предложения
- `GET /proposals` - список предложений
- `GET /proposals/create` - форма создания КП
- `POST /proposals/create` - создание предложения
- `GET /proposals/{id}` - просмотр предложения
- `GET /proposals/{id}/edit` - форма редактирования
- `POST /proposals/{id}/edit` - обновление предложения
- `GET /proposals/{id}/pdf` - скачивание PDF
- `POST /proposals/{id}/delete` - удаление предложения

## 🎨 Дизайн и UX

- **Темная тема** с современным интерфейсом
- **Адаптивный дизайн** для всех устройств
- **Плавные анимации** и переходы
- **Интуитивная навигация**
- **Профессиональное оформление PDF**

## 🔧 Настройка

### Переменные окружения
```php
// В index.php можно настроить:
define('PROJECT_ROOT', __DIR__);
// Настройки базы данных в функции getDB()
```

### Кастомизация PDF
Шаблон PDF находится в функции `generateProposalPDF()` в `index.php`.

## 🐛 Устранение неполадок

### Проблемы с PDF
- Убедитесь, что установлены права на запись
- Проверьте наличие TCPDF: `composer install`

### Ошибки базы данных
- Система автоматически переключается на JSON хранилище
- Проверьте права доступа к файлам `*.json`

### Проблемы с загрузкой изображений
- Проверьте права на директорию `uploads/`
- Поддерживаемые форматы: JPG, PNG, GIF

## 📈 Будущие улучшения

- [ ] Система пользователей и аутентификация
- [ ] Интеграция с email для отправки КП
- [ ] Шаблоны предложений
- [ ] Аналитика и статистика
- [ ] API для интеграции с другими системами
- [ ] Многоязычность (i18n)

## 📄 Лицензия

Этот проект является демонстрационным и предназначен для образовательных целей.

## 👥 Автор

**Yaroslav** - Разработчик системы автоматизации коммерческих предложений

## 📞 Контакты

Для вопросов и предложений: [ваш email]

---

**Создано с ❤️ для автоматизации бизнес-процессов**
