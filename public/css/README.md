# Структура CSS

## Обзор

CSS файлы организованы в модульной структуре для лучшей поддерживаемости и производительности.

## Структура папок

```
css/
├── app.css              # Главный файл с импортами всех модулей
├── reset.css            # Базовые стили и сброс
├── layout.css           # Общий layout (навигация, header)
├── utilities.css        # Утилиты и responsive стили
├── components/          # Переиспользуемые компоненты
│   ├── buttons.css      # Стили кнопок
│   ├── forms.css        # Формы и модальные окна
│   └── cards.css        # Карточки товаров и предложений
└── pages/               # Страницы-специфичные стили
    ├── auth.css         # Авторизация и регистрация
    ├── products.css     # Каталог товаров
    └── proposals.css    # Коммерческие предложения
```

## Порядок импортов

Импорты в `app.css` организованы по принципу специфичности (от общего к частному):

1. **reset.css** - базовые стили
2. **layout.css** - общий layout
3. **components/** - переиспользуемые компоненты
4. **pages/** - страница-специфичные стили
5. **utilities.css** - утилиты и переопределения

## Использование

### Добавление новых стилей

1. **Для новых компонентов**: добавьте в соответствующий файл в `components/`
2. **Для новых страниц**: создайте новый файл в `pages/`
3. **Для утилит**: добавьте в `utilities.css`

### Примеры селекторов

```css
/* Компонентные стили */
.btn-primary { ... }          /* components/buttons.css */
.product-card { ... }         /* components/cards.css */
.form-input { ... }           /* components/forms.css */

/* Страничные стили */
.products-grid { ... }        /* pages/products.css */
.proposal-form { ... }        /* pages/proposals.css */
.auth-card { ... }            /* pages/auth.css */

/* Layout стили */
.navbar { ... }               /* layout.css */
.page-header { ... }          /* layout.css */

/* Утилиты */
.fade-in { ... }              /* utilities.css */
@media (max-width: 768px) { ... } /* utilities.css */
```

## Преимущества структуры

- 🎯 **Легкая навигация** - быстро найти нужный стиль
- 🔧 **Простая поддержка** - изменения изолированы
- 📦 **Модульность** - независимые компоненты
- 🚀 **Производительность** - лучшее кеширование
- 👥 **Командная разработка** - меньше конфликтов

## Соглашения по именованию

- **Классы**: kebab-case (`.product-card`, `.btn-primary`)
- **Файлы**: lowercase с подчеркиваниями (`buttons.css`, `auth.css`)
- **Комментарии**: описывают секции кода

## Добавление новых модулей

1. Создайте новый файл в соответствующей папке
2. Добавьте импорт в `app.css`
3. Следуйте соглашениям по именованию
4. Добавьте документацию в этот файл
