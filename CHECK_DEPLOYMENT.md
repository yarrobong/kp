# Проверка работы автоматического деплоя

## ✅ Что проверить:

### 1. Проверка workflow на GitHub

1. Откройте: https://github.com/yarrobong/kp/actions
2. Найдите последний запуск "Deploy to Server"
3. Кликните на него, чтобы увидеть детали

### 2. Возможные проблемы и решения:

#### Проблема: "Secret not found" или "Required secret is missing"

**Решение:** Убедитесь, что все секреты добавлены:
- `SERVER_HOST`
- `SERVER_PASSWORD`
- `DEPLOY_PATH`

Проверьте: https://github.com/yarrobong/kp/settings/secrets/actions

#### Проблема: "Connection refused" или "Permission denied"

**Решение:** 
1. Проверьте доступность сервера:
```bash
ping 178.209.127.17
ssh root@178.209.127.17
```

2. Убедитесь, что порт 22 (SSH) открыт

3. Проверьте правильность пароля в секрете `SERVER_PASSWORD`

#### Проблема: "git: command not found" на сервере

**Решение:** Установите git на сервере:
```bash
ssh root@178.209.127.17
# Для CentOS/RHEL:
yum install git -y
# Для Ubuntu/Debian:
apt-get update && apt-get install git -y
```

#### Проблема: "No such file or directory" для DEPLOY_PATH

**Решение:** Создайте директорию на сервере:
```bash
ssh root@178.209.127.17
mkdir -p /var/www/html
# Или используйте другой путь и обновите секрет DEPLOY_PATH
```

#### Проблема: "Repository not found" или "git clone failed"

**Решение:** Убедитесь, что на сервере есть доступ к GitHub:
```bash
ssh root@178.209.127.17
cd /var/www/html
git clone https://github.com/yarrobong/kp.git .
```

### 3. Ручная проверка деплоя

Выполните команды вручную на сервере, чтобы убедиться, что все работает:

```bash
# Подключитесь к серверу
ssh root@178.209.127.17

# Перейдите в директорию проекта
cd /var/www/html

# Проверьте, что git репозиторий настроен
git status

# Попробуйте обновить код вручную
git fetch origin
git reset --hard origin/main

# Проверьте права доступа
ls -la storage bootstrap/cache
chmod -R 755 storage bootstrap/cache
```

### 4. Проверка логов workflow

В GitHub Actions:
1. Откройте запуск workflow
2. Кликните на шаг "Deploy to server"
3. Посмотрите логи выполнения
4. Найдите строку с ошибкой и исправьте проблему

### 5. Тестовый запуск

После исправления проблем сделайте тестовый коммит:

```bash
git add .
git commit -m "Test deployment after fixes"
git push origin main
```

Затем проверьте статус в: https://github.com/yarrobong/kp/actions

## 📊 Статус проверки

- [ ] Workflow запускается при push
- [ ] Все секреты настроены
- [ ] Сервер доступен по SSH
- [ ] Git установлен на сервере
- [ ] Директория DEPLOY_PATH существует
- [ ] Права доступа настроены правильно
- [ ] Деплой выполняется успешно

## 🔍 Быстрая диагностика

Если workflow падает с ошибкой, проверьте:

1. **Секреты:** https://github.com/yarrobong/kp/settings/secrets/actions
2. **Логи workflow:** https://github.com/yarrobong/kp/actions (кликните на последний запуск)
3. **Доступность сервера:** `ssh root@178.209.127.17`
4. **Git на сервере:** `ssh root@178.209.127.17 "git --version"`

