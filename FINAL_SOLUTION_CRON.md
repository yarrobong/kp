# ✅ ФИНАЛЬНОЕ РЕШЕНИЕ: Используйте Cron вместо Webhook

## Проблема

Webhook не работает из-за проблем с конфигурацией веб-сервера (404/500 ошибки). 

## Решение: Cron Job (Рекомендуется)

Cron автоматически проверяет GitHub каждые 5 минут и обновляет код при изменениях. Это проще и надежнее, чем webhook.

## Установка на сервере

Выполните на сервере **ОДНУ команду**:

```bash
ssh root@178.209.127.17 << 'EOF'
cd /tmp
wget -q https://raw.githubusercontent.com/yarrobong/kp/main/deploy-cron.sh
chmod +x deploy-cron.sh
bash deploy-cron.sh /var/www/html root
EOF
```

Или выполните команды по отдельности:

```bash
ssh root@178.209.127.17

cd /tmp
wget https://raw.githubusercontent.com/yarrobong/kp/main/deploy-cron.sh
chmod +x deploy-cron.sh
sudo bash deploy-cron.sh /var/www/html root
```

## Что делает скрипт

1. ✅ Клонирует/обновляет git репозиторий в `/var/www/html`
2. ✅ Создает скрипт деплоя `/usr/local/bin/deploy-kp.sh`
3. ✅ Настраивает cron задачу (проверка каждые 5 минут)
4. ✅ Устанавливает зависимости через composer
5. ✅ Настраивает права доступа

## Проверка работы

После установки:

```bash
# Проверьте cron задачу
crontab -l

# Посмотрите логи
tail -f /var/log/deploy-kp.log

# Запустите вручную для теста
/usr/local/bin/deploy-kp.sh
```

## Как это работает

- Каждые 5 минут cron проверяет GitHub
- Если есть новые коммиты - автоматически обновляет код
- Логи сохраняются в `/var/log/deploy-kp.log`

## Преимущества Cron над Webhook

- ✅ Не требует настройки веб-сервера
- ✅ Не требует публичного URL
- ✅ Работает автоматически
- ✅ Проще настроить
- ✅ Надежнее

## Изменение интервала проверки

Если хотите проверять чаще (например, каждую минуту):

```bash
ssh root@178.209.127.17
crontab -e

# Измените строку:
# Было: */5 * * * * /usr/local/bin/deploy-kp.sh
# Стало: * * * * * /usr/local/bin/deploy-kp.sh  (каждую минуту)
```

## Отключение Webhook в GitHub (опционально)

Если больше не нужен webhook:

1. Откройте: https://github.com/yarrobong/kp/settings/hooks
2. Найдите ваш webhook
3. Нажмите "Delete"

## Готово! 🎉

После выполнения команды установки код будет автоматически обновляться каждые 5 минут при каждом push в GitHub!

